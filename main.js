'use strict'

const km = prompt ('Insert the distance you want to travel in Km');
const age = prompt ('Insert your age');

let nKm =parseInt(km)

 let basePrice = (nKm *= 0.21);

 if (age < 18){ let revPrice = (basePrice / 0.8)}

 console.log(revPrice)